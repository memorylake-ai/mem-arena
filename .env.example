# Placeholder env for Docker/build only. Next build loads routes that import code reading process.env;
# these values are never used at runtime (Helm mounts real .env into the container).
# Keep in sync with Helm values and any module that reads env at import time.

DATABASE_URL=postgres://localhost:5432/build
DOTENV_PUBLIC_KEY=
MEM0_API_KEY=build
SUPERMEMORY_API_KEY=build
ZOOTOPIA_API=http://localhost:8000
ZOOTOPIA_API_KEY=build
LITELLM_API=http://localhost:4000
LITELLM_API_KEY=build
ARENA_API_BASE=http://localhost:8001
MAIN_DOMAIN_API_URL=https://example.com
